# Предзаготовленные дизайны каруселей

## Обзор

В проект добавлена система предзаготовленных дизайнов с красивыми подложками, градиентами и паттернами.

## Где находятся шаблоны

### Основные файлы:

1. **[src/lib/background-patterns.ts](src/lib/background-patterns.ts)** - паттерны, градиенты и готовые дизайны
2. **[src/components/design-template-selector.tsx](src/components/design-template-selector.tsx)** - UI компонент для выбора дизайнов
3. **[src/components/elements/background-layer.tsx](src/components/elements/background-layer.tsx)** - компонент фона (обновлен для поддержки градиентов)
4. **[src/components/settings-panel.tsx](src/components/settings-panel.tsx)** - добавлена новая вкладка "Design"

## Предзаготовленные дизайны

### 1. Starry Night 🌟
- **Описание**: Синий градиент со звездами
- **Использование**: Технические и обучающие посты
- **Цвета**: Фиолетово-синий градиент
- **Шрифты**: Montserrat, Open Sans

### 2. Purple Waves 🌊
- **Описание**: Современный фиолетовый с волнами
- **Использование**: Креативные и современные посты
- **Цвета**: Фиолетово-голубой градиент
- **Шрифты**: Poppins, Inter

### 3. Notebook Style 📝
- **Описание**: Минималистичный стиль тетради
- **Использование**: Образовательный контент
- **Цвета**: Бежевая подложка с сеткой
- **Шрифты**: Caveat, Patrick Hand

## Как использовать

### В интерфейсе:

1. Откройте редактор каруселей
2. Перейдите на новую вкладку **"Design"** ✨ (первая слева)
3. Выберите один из 3 готовых дизайнов или отдельный градиент
4. Дизайн автоматически применится ко всем слайдам

### Программно:

```typescript
import { designTemplates, backgroundPatterns } from "@/lib/background-patterns";

// Получить список всех шаблонов
const templates = designTemplates;

// Применить шаблон
const template = designTemplates[0]; // Starry Night
form.setValue("config.theme.background", backgroundPatterns[template.backgroundPattern].value);
form.setValue("config.theme.primary", template.primaryColor);
```

## Доступные градиенты

Помимо 3 основных дизайнов, доступны дополнительные градиенты:

- **Sunset Glow** 🌅 - Красно-желтый закат
- **Ocean Breeze** 🌊 - Океанский синий
- **Forest Dream** 🌲 - Зеленый лес
- **Pink Cloud** ☁️ - Розовые облака
- **Dark Mode** 🌙 - Темный режим
- **Mint Fresh** 🍃 - Свежая мята
- **Royal** 👑 - Королевский синий

## Добавление собственных дизайнов

### Шаг 1: Добавьте градиент в `background-patterns.ts`

```typescript
export const backgroundPatterns: Record<string, BackgroundPattern> = {
  // ... существующие паттерны

  myCustomGradient: {
    name: 'My Custom Gradient',
    type: 'gradient',
    value: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
    textColor: '#ffffff',
    preview: '🎨',
  },
};
```

### Шаг 2: Создайте полный дизайн-шаблон

```typescript
export const designTemplates: DesignTemplate[] = [
  // ... существующие шаблоны

  {
    id: 'my-custom-design',
    name: 'My Custom Design',
    description: 'Описание вашего дизайна',
    backgroundPattern: 'myCustomGradient',
    overlayPattern: 'stars', // опционально
    primaryColor: '#ffffff',
    secondaryColor: '#e0e0e0',
    textColor: '#ffffff',
    font1: 'Montserrat',
    font2: 'Open_Sans',
    preview: '🎨',
  },
];
```

### Шаг 3: Добавьте SVG паттерн (опционально)

```typescript
export const overlayPatterns = {
  // ... существующие паттерны

  myPattern: {
    name: 'My Pattern',
    svg: `data:image/svg+xml,%3Csvg width='100' height='100' xmlns='http://www.w3.org/2000/svg'%3E...%3C/svg%3E`,
  },
};
```

## Структура файлов

```
src/
├── lib/
│   ├── background-patterns.ts        # Все паттерны и градиенты
│   ├── default-document.tsx          # Конфигурация по умолчанию
│   ├── default-slides.tsx            # Типы слайдов
│   ├── pallettes.tsx                 # Цветовые палитры
│   └── themes.ts                     # Темы DaisyUI
├── components/
│   ├── design-template-selector.tsx  # Селектор шаблонов
│   ├── settings-panel.tsx            # Панель настроек (добавлена вкладка Design)
│   └── elements/
│       └── background-layer.tsx      # Компонент фона (поддержка градиентов)
```

## Пример использования в коде

```typescript
// Применить готовый дизайн
import { designTemplates, backgroundPatterns } from "@/lib/background-patterns";

const applyStarryNightDesign = () => {
  const template = designTemplates.find(t => t.id === 'starry-night-blue');
  const bgPattern = backgroundPatterns[template.backgroundPattern];

  setValue("config.theme.background", bgPattern.value);
  setValue("config.theme.primary", template.primaryColor);
  setValue("config.theme.isCustom", true);
};

// Или применить только градиент
import { backgroundPatterns } from "@/lib/background-patterns";

const applyGradient = () => {
  setValue("config.theme.background", backgroundPatterns.purpleWaves.value);
};
```

## CSS градиенты

Компонент `BackgroundLayer` теперь поддерживает:

- ✅ Цвета (`#ffffff`)
- ✅ Линейные градиенты (`linear-gradient(...)`)
- ✅ Радиальные градиенты (`radial-gradient(...)`)
- ✅ SVG паттерны через data URI
- ✅ Комбинацию градиента + паттерна

## Возможности

1. **3 готовых дизайна** с подложками как на примерах
2. **10+ градиентов** для быстрого выбора
3. **SVG паттерны** (звезды, волны, сетка, точки)
4. **Легко расширяемая система** - просто добавьте новый объект в массив
5. **Визуальный выбор** - превью каждого дизайна
6. **One-click применение** - клик по карточке применяет весь дизайн

## Следующие шаги

Возможные улучшения:

1. Добавить больше готовых дизайнов
2. Поддержка загрузки собственных паттернов
3. Редактор градиентов в UI
4. Сохранение пользовательских дизайнов
5. Экспорт/импорт дизайнов
6. Анимированные градиенты

## Скриншоты

Новая вкладка "Design" появляется первой в панели настроек слева с иконкой ✨ (Sparkles).
